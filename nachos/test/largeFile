
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );

	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
	 // printf( "cannot get the right descriptor" );
